# profiles/ (unused in v1)

Tor "Ghost" profile automation was planned here but is **not shipped** in the first
release. First-boot init provisioning was removed; Setup handles onboarding instead.

Milestone 2 may recreate Ghost as a Setup-created secondary profile. Until then this
directory stays empty on purpose.
