# Deprecated stub

The real WebUSB installer lives at [`../site/install/`](../site/install/).

Do not use or advertise `distribution/web-installer/index.html` — it is an obsolete
placeholder kept only so old links do not 404 in git history.
